/** Must stay in sync with Telegram callback_data size (64 bytes) when paired with a UUID in `value`. */
export const COLLECTION_SELECT_ACTION_ID = 'sel_collection';

/** Value is the next page number (integer string). */
export const COLLECTIONS_LOAD_MORE_ACTION_ID = 'load_more_collections';
